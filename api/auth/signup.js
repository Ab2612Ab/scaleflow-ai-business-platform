import {json,method,supabase,errorMessage} from '../_config.js';
export default async function handler(req,res){if(!method(req,res,'POST'))return;try{const {email,password,name}=req.body||{};if(!email||!password||password.length<8)return json(res,400,{error:'Enter a valid email and a password of at least 8 characters.'});const data=await supabase('/auth/v1/signup',{method:'POST',body:{email,password,data:{full_name:name||''}}});json(res,200,{session:data.access_token?{access_token:data.access_token,refresh_token:data.refresh_token,user:data.user}:null,user:data.user||null})}catch(e){json(res,400,{error:errorMessage(e)})}}
