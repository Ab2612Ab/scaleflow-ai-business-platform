import {json,method,errorMessage} from './_config.js';
export default async function handler(req,res){if(!method(req,res,'POST'))return;try{const {name,email,company,service,message}=req.body||{};if(!name||!email||!message)return json(res,400,{error:'Name, email and message are required.'});const key=process.env.RESEND_API_KEY;const to=process.env.CONTACT_TO_EMAIL;const from=process.env.CONTACT_FROM_EMAIL;if(!key||!to||!from)throw new Error('Contact email backend is not configured.');const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from,to,reply_to:email,subject:`New ScaleFlow inquiry — ${company||'New prospect'}`,html:`<h2>New project inquiry</h2><p><b>Name:</b> ${escapeHtml(name)}</p><p><b>Email:</b> ${escapeHtml(email)}</p><p><b>Company:</b> ${escapeHtml(company||'Not provided')}</p><p><b>Service:</b> ${escapeHtml(service||'Not selected')}</p><p><b>Message:</b></p><p>${escapeHtml(message).replace(/\n/g,'<br/>')}</p>`})});if(!r.ok){const d=await r.json().catch(()=>({}));throw new Error(d.message||'Email delivery failed.')}json(res,200,{ok:true})}catch(e){json(res,500,{error:errorMessage(e)})}}
function escapeHtml(value=''){return String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
